# Acceptance: launch (increments 1 through 8, one deploy)

**Date run:** 2026-09-08, 00:30–01:05 UTC
**Runbook:** `docs/deploy/2026-09-10-launch.md` (executed; this is its record), with the seven
increment runbooks it lists folded into one deploy
**Deployed commit:** `ac5959e` on `main` (release `v1.0.0` is `a83d1c5`; `ac5959e` adds only
the Discord ids to three runbooks)
**Migrations:** 0020 through 0028, applied together with the one-off runner;
`drizzle.__drizzle_migrations` now 29 rows, newest `1788798688820` = 0028's journal `when`
**Launch instant:** `2026-09-08T00:39:17Z` — season 1's `started_at`, used verbatim as
`pnpm launch --at`
**Operator:** Claude, over SSH as `acab@regime`, on the user's instruction

## Why one deploy, not nine

The pre-deploy survey found `factions_live` at migration 0019: increment 1's 0020 had never
been applied, so the pending set was 0020–0028. The database held 0 factions, 0 members,
0 poles, 3 identity links and 1551 events. With nothing to lose in the one irreversible
migration (0020's drop of `factions.pole_key/x/y/z` moves rows of a table that had none)
and every other migration additive, the nine runbooks' migration, seed and restart steps
were run once, in their own order, with the bot stopped for the migrations and the seeds.
Each runbook's manual prerequisites and acceptance checks were still done individually.

## Pre-deploy state

- Host checkout `bfd39f4`; bot process running since 2026-09-03 (older code); web image 3
  days old, worker image 4 days old.
- `select count(*), max(created_at) from drizzle.__drizzle_migrations` → `20 | 1788463371578`
  (exactly 0019's `when`, so the migrator would apply 0020–0028 and nothing older).
- 0020 precheck (`factions` with a status and no `ceremony_id`) → zero rows.
- Backup: `~/clan-wars-pre-launch-20260908-0038.sql` on the host, 1,001,620 bytes, 21
  `COPY public.` blocks including `factions` and `poles`. This is the only recovery path
  for 0020 and it was taken before any statement ran.

## What was done, in order

1. `sudo systemctl stop clan-wars-bot`; `git pull --ff-only` to `ac5959e`; `pnpm install
   --frozen-lockfile`.
2. Full `pg_dump` (above).
3. Migrations 0020–0028 via the runner from `docs/deploy/2026-09-02-dormancy.md`. Verified:
   `declarations`, `clan_notices`, `seasons`, `clan_pins`, `kills`, `vault_locks` exist;
   `factions` has no `pole_key`/`x`/`y`/`z`.
4. Runbook seeds: 3a's dormant-reason stamp (0 rows), season 1 opened
   (`started_at 2026-09-08 00:39:17.614296+00`), `raid-consumer`/`raise-consumer` at the
   log head (1551); 5's `zone-watch` at 1551 and `positions-projector` at 0 (every event is
   inside the 30-day window); 6's `sessions-projector`/`kills-projector` confirmed absent.
5. nginx: `nginx -t` ok, `reload` (the vhost symlink now carries `location /tiles/`;
   `/var/www/tiles/enoch/topographic/0/0/0.webp` present).
6. Map runbook step 1: the six newest structure lines are all
   `)Built <part> on Fence with <tool>` — the regex's `\s*` form. No gate line has been
   logged on this server yet; the parser accepts the `Fence` lines, and a gate is the
   same grammar. Not stopped on.
7. Game server, through the Nitrado API (read, then one delete): `cfggameplay.json`
   `WorldsData.objectSpawnersArr` = `bunker-enhancements.json`, `faction-supplies.json`,
   `teleports.json` — the stale `custom/flag-supplies.json` (30,247 bytes) was NOT named,
   so it was backed up locally and deleted; `custom/` listing afterwards has
   `faction-supplies.json` and no `flag-supplies.json`. `GeneralData.disableBaseDamage` =
   `true`, correct for a Monday (`docs/deploy/raid-window.md`). `db/globals.xml`
   `FlagRefreshMaxDuration` = `604800` = 7 days. No game-server restart: nothing it loads
   changed.
8. Bot started on `ac5959e`. First ticks: `bot ready as Clan Wars#3900`, `guild members
   fetched: 11`, `structure on start: linkedAdds 3`, `pole projection: 14 poles`,
   `positions: 1237 fix(es)`. No `failed` line.
9. `docker compose build web ingest-worker`, then `up -d web ingest-worker` (named
   services). Both `running`; web logs `Ready`; worker sweeping (`ingest sweep: 1 servers
   in ~2 s`).
10. Grace stamp, bot stopped: `pnpm launch --server 1 --at 2026-09-08T00:39:17Z` →
    `{"polesStamped":4,"graceUntil":"2026-09-15T00:39:17.000Z"}`. Bot started again.

## Acceptance (launch runbook §4)

| Check | Result |
|---|---|
| `select count(*) from factions` | 0 |
| Dormancy check (`declarations` join) | zero rows — read with the count above: nothing exists to transition |
| `select count(*) from declarations` | 0 |
| `seasons` for server 1 | one row: number 1, `started_at 2026-09-08 00:39:17.614296+00`, `ended_at` null |
| Grace | `in_grace 4, total 4, min = max = 2026-09-15 00:39:17+00` |
| Queues (`faction_events`, `clan_notices`, `war_log_events` unposted) | 0, 0, 0 |
| Consumer cursors | all eleven at 1551 = `max(events.id)` |
| Discord | `#clan-feed`, `#war-log` present; `CLANS` and `CLAN VOICE` empty; `@Linked` (3 holders), `@Alpha` (0) below the bot's role; registered commands: `guest` live, `link`/`unlink`/`whoami`/`faction` answering "Retired — manage this on the site." |
| Site, signed out | `/`, `/clans`, `/scoreboard`, `/alphas`, `/seasons`, `/war-log`, `/players` → 200; `/guide` → 308 to `https://fieldguide.dayzclanwars.com/`; `/guide/05-raiding.html` → 308 to `…/05-raiding.html`; `/me`, `/map`, `/clan` → 303 to `/login?next=…` |
| `pnpm guide:numbers` then `git diff --stat docs/guide-numbers.json` | only `generatedAt` changes |

## Not exercised, and what to expect

- No raid, defense, vote, vault, guest pass, claim, join or nickname prefix — none can
  happen with zero clans. The first ceremony is the first real test of 2c-b through 7.
- `player_sessions` and `kills` are both 0 after a full backfill: the 1551 events hold no
  `player.connected`/`player.killed` rows because those parsers arrived in increment 6 and
  the 5112 `raw_lines` already ingested were parsed before them. New ADM files will produce
  them. A re-parse of the stored raw lines is a follow-up, not a launch blocker.
- Signed-in checks (`/me` gamertag, `/map` travel points, `/link` already-linked copy) were
  not run — no browser session on the host; the middleware's 303s prove the gate.
- The web-only `.env` values were left as they were; the staged `.env.new` on the host is
  identical to `.env` and was not swapped.

## Standing obligations from here

- `docs/deploy/raid-window.md`: flip `disableBaseDamage` to `false` on Friday 00:00 UTC,
  `true` on Monday 00:00 UTC, restart each time. ~~Next flip: Friday 2026-09-11 00:00 UTC.~~
  Amended 2026-09-10: the first raid weekend is delayed one week by decision; the 2026-09-11
  and 2026-09-14 flips are skipped and the first window opens Friday 2026-09-18 00:00 UTC
  (`docs/deploy/raid-window.md`, "Exceptions"). Grace, scoring and the week close are unchanged.
- The week tick closes the first (partial) week at Monday 2026-09-14 00:00 UTC.
- The wipe, at season end: `docs/deploy/2026-09-06-scoring-and-seasons.md` and `pnpm wipe`.
